{% extends 'base.html.twig' %}

{% block body %}
    <h2>Products</h2>
    <a href="{{ path('newproduct') }}">Create new</a>
    <ul>
    {% for product in products %}
        <li>{{ product.name }}</li>
    {% endfor %}
    </ul>
    {{ dump(products) }}
{% endblock %}